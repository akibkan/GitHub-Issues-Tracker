the difference between var, let, and const is there scope, behaviour of hoisting,and re-assignment rules.first-of-all var is global scope and this var allows re-declaration. let variables declares block scope, it allows updated but no declaration the same block. then the const variables with block scoped but they cannot be reassigned same block.                                                                                                                                 
The spread operator is used to spread an array or object into separate parts.It helps make code readable .  This operator is widely used for cloning, merging, and passing values.                                                                                                                                           
Map returns a new array, modifies each element of the array,and map uses value,index,array. Filter returns a new array and filter out the element which passes the condition and its uses current value,index and array. Reduce returns a single element and array to a single value.                                                                                                                                            
Arrow function is a concise way to  writ function using the => syntax, and then automatically binding this from the full context.                                                                                                                                          
template literals are strings written using backtick that allow variables and expressions to be embedded directly.